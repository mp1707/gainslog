import { StyleSheet } from 'react-native';
import { spacing } from '../../../theme';

export const styles = StyleSheet.create({
  button: {
    position: 'absolute',
    bottom: spacing['4xl'] + spacing.xs,
    right: spacing.padding.large,
  },
});